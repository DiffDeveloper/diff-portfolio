// Kept as a tiny standalone module: the 2D site dispatches this event without
// importing (and therefore bundling) the lazy-loaded 3D game chunk.
export const ENTER_GAME_EVENT = "diff:enter-game-mode";
